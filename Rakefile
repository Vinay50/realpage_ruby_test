desc 'Use the RealpageCLIRPNCalculator'
task :console do
  require_relative './bin/boot'

  calculator = RealpageCLIRPNCalculator.new({ input: 'console', output: 'console' })
  calculator.welcome_message
  calculator.start
end
